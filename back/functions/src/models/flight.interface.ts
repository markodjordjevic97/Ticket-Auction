interface Flight {
    origin: string;
    destination: string;
    soldSeatsBC: number;
    numberBC: number;
    travelDate: string;  // Date(travelDate)
    auctionStarted: boolean;
}

export default Flight;